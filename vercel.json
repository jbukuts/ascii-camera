{
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Cross-Origin-Opener-Policy",
          "value": "same-origin"
        },
        {
          "key": "Cross-Origin-Embedder-Policy",
          "value": "require-corp"
        }
      ]
    }
  ],
  "installCommand": "chmod +x ./bin/install-rust.sh && ./bin/install-rust.sh && npm ci",
  "buildCommand": "npm run compile:release && npm run build"
}
